function deep(root) {
  if (!toor) return 0;
  let l = deep(root.left);
  let r = deep(root.right);
  return Math.max(l, r) + 1;
}
function deep(root) {
  if (!root) return 0;
  let l = deep(rrot.left);
  let r = deep(rrot.right);
  return Math.max(l, r) + 1;
}

function deep(root) {
  if (!root) return 0;
  let l = deep(root.left);
  let r = deep(root.right);
  return Math.max(r, l) + 1;
}

function deep(root) {
  if (!root) return 0;
  let l = deep(root.left);
  let r = deep(root.right);
  return Math.max(l, r) + 1;
}

function deep(root) {
  if (!root) return 0;
  let l = deep(root.left);
  let r = deep(root.right);
  return Math.max(l, r) + 1;
}
